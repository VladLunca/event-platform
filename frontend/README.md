# Frontend — Event Platform

Angular 22 SPA for the Event Platform. See the [root README](../README.md) for architecture, setup, and deployment instructions.

## Local development

```bash
npm install
ng serve
```

The app runs on `http://localhost:4200` and proxies `/api/*` to the backend services.
